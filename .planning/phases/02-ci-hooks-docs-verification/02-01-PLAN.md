---
phase: 02-ci-hooks-docs-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_common.py
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-01, CI-01, CI-02, CI-03, CI-04]
must_haves:
  truths:
    - "pytest collects and passes tests/test_common.py exercising common/ stubs"
    - "coverage measures only banking_client and common, excluding mcp_server and agent"
    - "coverage report --fail-under=80 exits 0 on the current codebase"
    - "GitHub Actions workflow named CI triggers on push to main and PRs targeting main"
    - "CI runs ruff check, ruff format --check, mypy --strict, and pytest with coverage on Python 3.12 and 3.13"
    - "CI fails when coverage drops below 80 via a separate coverage report step"
  artifacts:
    - tests/test_common.py
    - .github/workflows/ci.yml
    - "pyproject.toml [tool.coverage.run] and [tool.coverage.report] sections"
  key_links:
    - "tests/test_common.py imports from common.* — exercises real code so coverage gate has something to measure"
    - "pyproject.toml [tool.coverage.run] omit excludes empty packages — without it, coverage measures wrong files (D-11)"
    - "CI test job runs uv run pytest --cov then uv run coverage report --fail-under=80 as separate steps (D-13)"
---

<objective>
Deliver the end-to-end verification slice: a reviewer pushes to GitHub and watches CI pass green on Python 3.12 and 3.13, running ruff, mypy --strict, and pytest with an 80% coverage gate. This is the vertical "push → CI green" path — a failing trivial test starts it, coverage config makes it measurable on real code only, and the CI workflow runs the whole pipeline.

Purpose: Prove the pytest + coverage pipeline works end-to-end (TEST-01) and that CI enforces all quality gates on a matrix (CI-01–04). This is the heart of the phase's core value.
Output: tests/test_common.py, coverage config in pyproject.toml, .github/workflows/ci.yml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ci-hooks-docs-verification/02-CONTEXT.md
@.planning/phases/02-ci-hooks-docs-verification/02-RESEARCH.md
@.planning/phases/02-ci-hooks-docs-verification/02-PATTERNS.md
@pyproject.toml
@src/common/__init__.py
@src/common/config.py
@src/common/errors.py
@src/common/logging.py
</context>

## Artifacts this phase produces

New symbols and files created by Plan 01 (excluded from drift verification):

- `tests/test_common.py` — new file with test functions: `test_version_is_string`, `test_get_config_returns_default`, `test_get_logger_returns_logger`, `test_error_hierarchy`
- `.github/workflows/ci.yml` — new file; CI workflow named `CI` with jobs `lint`, `type-check`, `test`
- `pyproject.toml` new sections: `[tool.coverage.run]` (keys `source`, `omit`), `[tool.coverage.report]` (key `show_missing`)

Existing symbols this plan imports (already created in Phase 1, NOT new): `common.__version__`, `common.OpenBankingError`, `common.config.get_config`, `common.errors.ConfigurationError`, `common.errors.AuthenticationError`, `common.logging.get_logger`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing trivial test exercising common/ stubs</name>
  <files>tests/test_common.py</files>
  <read_first>
    - src/common/__init__.py (exports __version__ and OpenBankingError)
    - src/common/config.py (get_config signature)
    - src/common/errors.py (OpenBankingError, ConfigurationError, AuthenticationError hierarchy)
    - src/common/logging.py (get_logger signature)
    - .planning/phases/02-ci-hooks-docs-verification/02-PATTERNS.md (import + type-annotation + docstring conventions for the test file)
    - pyproject.toml (ruff lint rules E/F/I/B/UP/SIM/D, google docstring convention, mypy strict)
  </read_first>
  <behavior>
    - test_version_is_string: common.__version__ is a str instance
    - test_get_config_returns_default: get_config("missing_key", "fallback") returns "fallback"
    - test_get_logger_returns_logger: get_logger("test").name equals "test"
    - test_error_hierarchy: ConfigurationError and AuthenticationError are both subclasses of OpenBankingError
  </behavior>
  <action>
    Create tests/test_common.py as the project's first test. Start it with a module-level Google-style docstring and `from __future__ import annotations`. Import directly from the package namespace (not `src.`-prefixed): `from common import OpenBankingError, __version__`, `from common.config import get_config`, `from common.errors import AuthenticationError, ConfigurationError`, `from common.logging import get_logger`. Write four test functions, each annotated `-> None` (mypy --strict requires return annotations): `test_version_is_string`, `test_get_config_returns_default`, `test_get_logger_returns_logger`, `test_error_hierarchy`, asserting the behaviors above. Each function needs a one-line Google-style docstring to satisfy ruff D rules. Per D-12, these target the common/ stubs (config, logging, errors) so the coverage gate measures real code. RED→GREEN: the stubs already exist, so the test passes once written — run it to confirm collection and pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_common.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_common.py contains `from __future__ import annotations`
    - tests/test_common.py contains `def test_version_is_string() -> None:`
    - tests/test_common.py contains `def test_error_hierarchy() -> None:`
    - `uv run pytest tests/test_common.py -v` exits 0 with 4 passed
    - `uv run mypy` exits 0 (test file passes strict typing)
    - `uv run ruff check tests/test_common.py` exits 0
  </acceptance_criteria>
  <done>The four tests collect and pass under pytest, and the test file passes mypy --strict and ruff lint.</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage config measuring only real-code packages</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (existing [tool.X] sections — append new sections after them, following the same convention)
    - .planning/phases/02-ci-hooks-docs-verification/02-RESEARCH.md (Pattern 3: Coverage Source Configuration, D-11/D-13 rationale)
  </read_first>
  <action>
    Append two new sections to pyproject.toml after the existing `[tool.pytest.ini_options]` section. Add `[tool.coverage.run]` with `source = ["src"]` and `omit = ["src/mcp_server/*", "src/agent/*"]` — this implements D-11, measuring only banking_client and common (the packages with real code) and excluding the intentionally empty mcp_server and agent packages. Add `[tool.coverage.report]` with `show_missing = true`. Do NOT add `--cov-fail-under` to pytest addopts — per D-13 the 80% gate is a separate CI step, not a pytest flag, so reporting can run even when below threshold. Do not duplicate any ruff/mypy config; only add the two coverage sections.
  </action>
  <verify>
    <automated>uv run pytest --cov && uv run coverage report --fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `[tool.coverage.run]`
    - pyproject.toml contains `source = ["src"]`
    - pyproject.toml contains `"src/mcp_server/*"` and `"src/agent/*"` in the omit list
    - pyproject.toml contains `[tool.coverage.report]` with `show_missing = true`
    - `grep -c 'cov-fail-under' pyproject.toml` returns 0 (gate is CI-only, D-13)
    - `uv run pytest --cov` exits 0 and the coverage report lists only banking_client and common files (no mcp_server or agent)
    - `uv run coverage report --fail-under=80` exits 0
  </acceptance_criteria>
  <done>Coverage measures only banking_client and common, the trivial test produces >=80% coverage on those packages, and `coverage report --fail-under=80` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Add GitHub Actions CI workflow with matrix and parallel jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/02-ci-hooks-docs-verification/02-RESEARCH.md (Code Examples: GitHub Actions CI Workflow; Open Questions 1 and 2)
    - .planning/phases/02-ci-hooks-docs-verification/02-CONTEXT.md (D-01, D-02, D-03, D-04, D-13)
    - pyproject.toml (tools read config from here; CI invokes tools without duplicating flags)
  </read_first>
  <action>
    Create .github/workflows/ci.yml. Name the workflow `CI` (D-02). Trigger on `push` to branch `main` and `pull_request` targeting `main` (D-01) — no other branches. Define three separate parallel jobs (D-03) on `ubuntu-latest`. Each job: `actions/checkout@v4`, then `astral-sh/setup-uv@v8` with `enable-cache: true` (D-04), then `uv sync --all-extras --dev`. The `lint` job runs `uv run ruff check .` then `uv run ruff format --check .` (CI-02; format check per RESEARCH Open Question 2) with NO Python matrix (ruff is Python-version-independent per RESEARCH Open Question 1). The `type-check` job (CI-03) has `strategy.matrix.python-version: ["3.12", "3.13"]`, passes `python-version: ${{ matrix.python-version }}` to setup-uv, and runs `uv run mypy`. The `test` job (CI-04) has the same 3.12/3.13 matrix and runs `uv run pytest --cov --cov-report=term-missing`, then a separate step named "Enforce coverage threshold" running `uv run coverage report --fail-under=80` (D-13 — separate step, not a pytest flag). Do not pass `--strict`, `--select`, or other config flags to the tools; they read pyproject.toml. The matrix on type-check and test satisfies CI-01 (3.12 and 3.13).
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert d['name']=='CI'; assert set(d['jobs'])=={'lint','type-check','test'}; assert d['jobs']['test']['strategy']['matrix']['python-version']==['3.12','3.13']; assert 'main' in d[True]['push']['branches']; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml parses as valid YAML
    - Workflow `name` is `CI`
    - Trigger includes `push` to `main` and `pull_request` to `main` only (no other branches)
    - Jobs are exactly `lint`, `type-check`, `test` (three separate parallel jobs)
    - `lint` job has no `strategy.matrix`; runs `uv run ruff check .` and `uv run ruff format --check .`
    - `type-check` and `test` jobs have `strategy.matrix.python-version: ["3.12", "3.13"]`
    - `type-check` job runs `uv run mypy`
    - `test` job runs `uv run pytest --cov ...` then a separate step running `uv run coverage report --fail-under=80`
    - Every job uses `actions/checkout@v4`, `astral-sh/setup-uv@v8` with `enable-cache: true`, and `uv sync --all-extras --dev`
    - `grep -c 'cov-fail-under' .github/workflows/ci.yml` returns 0 (the gate uses `coverage report --fail-under`, not a pytest flag)
  </acceptance_criteria>
  <done>ci.yml defines the CI workflow with lint/type-check/test jobs, a 3.12/3.13 matrix on type-check and test, push/PR-to-main triggers, uv caching, and a separate coverage-enforcement step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → CI runner | Workflow YAML and third-party actions execute on GitHub-hosted runners |
| PR author → CI | Untrusted PR content (title, body, branch name) reaches the workflow context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Third-party GitHub Actions (checkout, setup-uv) | medium | mitigate | Pin actions to major version tags (`@v4`, `@v8`); use only official GitHub/Astral-maintained actions |
| T-02-02 | Tampering | Workflow expression injection via PR title/body | low | accept | No `${{ github.event.* }}` untrusted fields interpolated into `run:` shell steps; only `matrix.python-version` (a controlled literal) is interpolated |
| T-02-03 | Information Disclosure | Secret leakage in CI logs | low | accept | No secrets or credentials used in this phase; CI runs only public tooling on public skeleton code |
</threat_model>

<verification>
- `uv run pytest --cov` exits 0; report shows only banking_client and common, coverage >=80%
- `uv run coverage report --fail-under=80` exits 0
- `uv run mypy` and `uv run ruff check .` and `uv run ruff format --check .` exit 0
- ci.yml parses as valid YAML with the expected job/matrix structure
</verification>

<success_criteria>
- The trivial test (TEST-01) passes and exercises real common/ code
- Coverage measures only banking_client and common, gate at 80% via separate step (D-11, D-13)
- CI workflow (CI-01–04) runs ruff, mypy --strict, and pytest+coverage on a 3.12/3.13 matrix, triggered on push/PR to main
</success_criteria>

<output>
Create `.planning/phases/02-ci-hooks-docs-verification/02-01-SUMMARY.md` when done
</output>
