---
phase: 02-ci-hooks-docs-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
  - README.md
  - LICENSE
  - CONTRIBUTING.md
  - .env.example
autonomous: true
requirements: [HOOK-01, HOOK-02, DOCS-01, DOCS-02, DOCS-03, CONF-01]
must_haves:
  truths:
    - "pre-commit run --all-files runs ruff lint, ruff format, and mypy and passes on the current code"
    - "the mypy pre-commit hook uses the project virtualenv (language: system) so it sees installed deps like mcp"
    - "README.md presents the project professionally with CI badge, description, quick start, dev setup, and architecture overview"
    - "LICENSE contains the MIT license text"
    - "CONTRIBUTING.md documents setup, PR workflow, Conventional Commits, and code style"
    - ".env.example documents the FDX base URL and auth stub key as placeholders without real secrets"
  artifacts:
    - .pre-commit-config.yaml
    - README.md
    - LICENSE
    - CONTRIBUTING.md
    - .env.example
  key_links:
    - "mypy hook entry is `uv run mypy` with language: system — without the project venv it cannot resolve mcp types (Pitfall 1, D-07)"
    - "ruff hooks come from astral-sh/ruff-pre-commit pinned by rev — mirrors CI lint/format (D-06)"
    - "README CI badge points at the .github/workflows/ci.yml workflow named CI"
---

<objective>
Provide the developer-workflow and contributor-facing surface of the phase: pre-commit hooks that mirror CI's ruff and mypy checks, plus the documentation and configuration files a reviewer expects in an enterprise-grade repo — README, MIT LICENSE, CONTRIBUTING guide, and .env.example.

Purpose: Pre-commit (HOOK-01/02) catches lint/type errors before commit so they never reach CI. Docs and licensing (DOCS-01/02/03, CONF-01) complete the "clone and understand" reviewer experience that is this milestone's core value.
Output: .pre-commit-config.yaml, README.md, LICENSE, CONTRIBUTING.md, .env.example.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ci-hooks-docs-verification/02-CONTEXT.md
@.planning/phases/02-ci-hooks-docs-verification/02-RESEARCH.md
@.planning/phases/02-ci-hooks-docs-verification/02-PATTERNS.md
@pyproject.toml
@README.md
</context>

## Artifacts this phase produces

New files created by Plan 02 (excluded from drift verification):

- `.pre-commit-config.yaml` — new file; hooks `ruff-check`, `ruff-format` (from astral-sh/ruff-pre-commit), and a local `mypy` hook
- `README.md` — replaces the existing one-line placeholder with structured sections
- `LICENSE` — new MIT license file
- `CONTRIBUTING.md` — new contributor guide
- `.env.example` — new config template (placeholders only, no secrets)

This plan adds `pre-commit` to `[project.optional-dependencies] dev` in pyproject.toml via `uv add --dev pre-commit` (Task 1). Note: Plan 01 also edits pyproject.toml (coverage sections). To avoid a write conflict, see the file-ownership note in Task 1.

<tasks>

<task type="auto">
  <name>Task 1: Add pre-commit config mirroring CI ruff + mypy checks</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - .planning/phases/02-ci-hooks-docs-verification/02-RESEARCH.md (Pattern 2: local mypy hook; Code Examples: Pre-commit Configuration; Pitfall 1)
    - .planning/phases/02-ci-hooks-docs-verification/02-CONTEXT.md (D-05, D-06, D-07)
    - pyproject.toml (tools read their config from here; hooks must not duplicate flags)
  </read_first>
  <action>
    Create .pre-commit-config.yaml with two repos. First repo: `https://github.com/astral-sh/ruff-pre-commit` pinned to `rev: v0.15.19` (D-06) with two hooks — `id: ruff-check` (with `args: [--fix]`) and `id: ruff-format`. Second repo: `local` with one hook — `id: mypy`, `name: mypy`, `entry: uv run mypy`, `language: system`, `types: [python]`, `require_serial: true` (D-07 — local hook using the project venv so mypy resolves installed deps like mcp, per Pitfall 1). Do NOT add a pytest hook — tests are CI-only (D-05). Do not pass `--strict` or `--select`; the tools read pyproject.toml.

    Install pre-commit as a dev dependency so `uv run mypy` and the framework are available: run `uv add --dev pre-commit`. FILE-OWNERSHIP NOTE: this mutates pyproject.toml/uv.lock, which Plan 01 also touches (coverage sections). Both plans are Wave 1. The executor running the wave must serialize the two pyproject.toml edits (apply Plan 01's coverage sections and this `uv add` without clobbering each other) — `uv add` appends to the dependency list and does not touch `[tool.coverage.*]`, so the edits are non-overlapping in content. After install, run `uv run pre-commit install` to register the git hook locally is optional for verification; `pre-commit run --all-files` does not require it.
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - .pre-commit-config.yaml parses as valid YAML
    - Contains repo `https://github.com/astral-sh/ruff-pre-commit` with hooks `ruff-check` and `ruff-format`
    - Contains a `local` repo hook with `id: mypy`, `entry: uv run mypy`, `language: system`
    - `grep -c 'pytest' .pre-commit-config.yaml` returns 0 (tests are CI-only, D-05)
    - pyproject.toml `[project.optional-dependencies] dev` list contains `pre-commit`
    - `uv run pre-commit run --all-files` exits 0 (all hooks pass on current code)
  </acceptance_criteria>
  <done>pre-commit runs ruff lint, ruff format, and mypy hooks and they all pass on the existing codebase; no pytest hook is present.</done>
</task>

<task type="auto">
  <name>Task 2: Write MIT LICENSE and .env.example config template</name>
  <files>LICENSE, .env.example</files>
  <read_first>
    - .planning/phases/02-ci-hooks-docs-verification/02-RESEARCH.md (Don't Hand-Roll: MIT license text; Security Domain V14 Configuration)
    - .planning/phases/02-ci-hooks-docs-verification/02-CONTEXT.md (CONF-01 scope, DOCS-02)
  </read_first>
  <action>
    Create LICENSE with the standard MIT License text (DOCS-02). Use copyright line `Copyright (c) 2025 Dave Moran` — year 2025 per PATTERNS.md, holder is the repo owner. Use the exact canonical MIT wording (permission grant, the "AS IS" warranty disclaimer paragraph). Do not paraphrase the license body.

    Create .env.example (CONF-01) documenting the two config vars as placeholders with no real secrets (Security Domain V14 — placeholders only): an FDX base URL variable (e.g. `FDX_BASE_URL=https://api.example.com/fdx`) and an auth stub key variable (e.g. `FDX_AUTH_KEY=your-stub-key-here`). Add a brief `# comment` above each line explaining its purpose. These are stub/placeholder values — the milestone uses no real credentials.
  </action>
  <verify>
    <automated>grep -q 'MIT License' LICENSE && grep -q 'AS IS' LICENSE && grep -q 'FDX_BASE_URL' .env.example && grep -q 'FDX_AUTH_KEY' .env.example && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE contains `MIT License`
    - LICENSE contains the `AS IS` warranty-disclaimer phrase
    - LICENSE contains `Copyright (c) 2025`
    - .env.example contains a `FDX_BASE_URL` placeholder line
    - .env.example contains a `FDX_AUTH_KEY` placeholder line
    - .env.example contains no real secret values (placeholders only)
  </acceptance_criteria>
  <done>LICENSE holds the canonical MIT text with a 2025 copyright line; .env.example documents the FDX base URL and auth stub key as placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Write structured README and CONTRIBUTING guide</name>
  <files>README.md, CONTRIBUTING.md</files>
  <read_first>
    - README.md (current one-line placeholder `# fs-monorepo` — replace it entirely)
    - .planning/phases/02-ci-hooks-docs-verification/02-CONTEXT.md (D-08, D-09, D-10)
    - .planning/PROJECT.md (project description, core value, constraints for README content)
    - .github/workflows/ci.yml (workflow name `CI` — referenced by the README CI badge; created in Plan 01)
    - pyproject.toml (quick-start commands: uv sync, uv run pytest, uv run mypy, uv run ruff)
  </read_first>
  <action>
    Replace the entire README.md (currently just `# fs-monorepo`) with a structured document (D-10): a top title and a GitHub Actions CI status badge (markdown image linking to the Actions tab for the `CI` workflow, e.g. `![CI](https://github.com/<owner>/<repo>/actions/workflows/ci.yml/badge.svg)` — use the actual owner/repo if discoverable from `git remote`, otherwise leave a clearly-marked placeholder), a project description (Open Banking MCP Capstone — sourced from PROJECT.md), a "## Quick Start" section (clone, `uv sync --all-extras --dev`), a "## Development" section (running checks: `uv run ruff check .`, `uv run mypy`, `uv run pytest --cov`, and `uv run pre-commit install`), and an "## Architecture" section describing the four src-layout packages (banking_client, mcp_server, agent, common) and noting that mcp_server and agent are intentionally empty skeletons for now. Include a "## License" line referencing MIT. Per DOCS-01, include a placeholder section for the capstone spec excerpt (e.g. a `## Capstone Spec` heading with a `<!-- spec excerpt placeholder -->` note).

    Create CONTRIBUTING.md as a full 2-3 page workflow guide (D-08): a "## Getting Started / Setup" section (`uv sync --all-extras --dev`, then `uv run pre-commit install`); a "## Development Workflow" section (branch, make changes, run `uv run pre-commit run --all-files`, open a PR targeting `main`); a "## Commit Conventions" section enforcing Conventional Commits with the prefixes `feat:`, `fix:`, `docs:`, `chore:` (D-09) and a short example of each; and a "## Code Style" section stating ruff (lint + format) and mypy --strict are the quality gates, all configured in pyproject.toml, mirrored by pre-commit and CI. Keep it substantive (enterprise contributor onboarding), not a stub.
  </action>
  <verify>
    <automated>grep -q '## Quick Start' README.md && grep -q '## Architecture' README.md && grep -qi 'badge.svg' README.md && grep -q '## Commit Conventions' CONTRIBUTING.md && grep -q 'feat:' CONTRIBUTING.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - README.md no longer equals the single line `# fs-monorepo`
    - README.md contains a CI badge (`badge.svg`), `## Quick Start`, `## Development`, and `## Architecture` sections
    - README.md contains a capstone spec excerpt placeholder (DOCS-01)
    - README.md mentions all four packages: banking_client, mcp_server, agent, common
    - CONTRIBUTING.md contains `## Commit Conventions` and references all four prefixes `feat:`, `fix:`, `docs:`, `chore:`
    - CONTRIBUTING.md contains setup (`uv sync`), workflow (PR to main), and code-style (ruff + mypy) sections
    - `uv run ruff format --check README.md CONTRIBUTING.md` is not required (markdown), but both files render as valid markdown
  </acceptance_criteria>
  <done>README.md is a structured portfolio-grade document with CI badge, quick start, development, architecture, and spec-excerpt placeholder; CONTRIBUTING.md is a full guide covering setup, PR workflow, Conventional Commits, and code style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → git | Pre-commit hooks execute local tooling before a commit is recorded |
| Repo files → reader | Documentation and .env.example are read by contributors and reviewers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | Malicious pre-commit hook repo | medium | mitigate | Pin `astral-sh/ruff-pre-commit` to `rev: v0.15.19`; use only the official Astral repo; mypy hook is `local` (no remote repo) |
| T-02-05 | Information Disclosure | Real secrets committed via .env.example | medium | mitigate | .env.example contains placeholder values only (V14 Configuration); real `.env` is gitignored; no credentials in the template |
</threat_model>

<verification>
- `uv run pre-commit run --all-files` exits 0 (ruff lint + format + mypy hooks pass)
- `grep -c 'pytest' .pre-commit-config.yaml` returns 0
- LICENSE contains MIT text; .env.example has FDX_BASE_URL and FDX_AUTH_KEY placeholders
- README.md and CONTRIBUTING.md contain the required sections
</verification>

<success_criteria>
- Pre-commit (HOOK-01/02) mirrors CI: ruff lint+format and mypy --strict via the project venv; no tests
- MIT LICENSE (DOCS-02), structured README with badge + spec placeholder (DOCS-01/D-10), full CONTRIBUTING guide with Conventional Commits (DOCS-03/D-08/D-09), and .env.example placeholders (CONF-01) all exist
</success_criteria>

<output>
Create `.planning/phases/02-ci-hooks-docs-verification/02-02-SUMMARY.md` when done
</output>

