---
phase: 01-package-skeleton-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/banking_client/__init__.py
  - src/banking_client/py.typed
  - src/mcp_server/__init__.py
  - src/mcp_server/py.typed
  - src/agent/__init__.py
  - src/agent/py.typed
  - src/common/__init__.py
  - src/common/py.typed
  - src/common/config.py
  - src/common/logging.py
  - src/common/errors.py
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-05
must_haves:
  truths:
    - "uv sync completes without errors and installs open-banking-mcp in a src-layout virtualenv"
    - "Four packages (banking_client, mcp_server, agent, common) import from a clean Python session after install"
    - "mypy --strict src/ reports zero errors on the skeleton"
    - "ruff check src/ and ruff format --check src/ report zero violations"
    - "from common import OpenBankingError succeeds (D-03 re-export)"
  artifacts:
    - pyproject.toml
    - uv.lock
    - src/banking_client/__init__.py
    - src/banking_client/py.typed
    - src/mcp_server/__init__.py
    - src/mcp_server/py.typed
    - src/agent/__init__.py
    - src/agent/py.typed
    - src/common/__init__.py
    - src/common/py.typed
    - src/common/config.py
    - src/common/logging.py
    - src/common/errors.py
  key_links:
    - "pyproject.toml [tool.hatch.build.targets.wheel] packages list -> hatchling discovers all four src/ packages"
    - "pyproject.toml [tool.mypy] mypy_path + packages -> mypy resolves cross-package imports under src/"
    - "importlib.metadata.version(\"open-banking-mcp\") in each __init__.py -> pyproject.toml [project] version field"
    - "common/__init__.py __all__ re-export -> common/errors.py OpenBankingError"
---

<objective>
Establish the four-package src-layout skeleton and all strict local tooling so a developer can clone the repo, run `uv sync`, and have `mypy --strict src/` and `ruff check src/` report zero errors before any business logic exists.

This is the Walking Skeleton for the capstone: the smallest end-to-end capability is "the repo's enterprise tooling passes green on an empty four-package skeleton." It proves the full local toolchain (build backend, type checker, linter/formatter, test runner config, dependency pinning) works together in a single `pyproject.toml`.

Purpose: Production posture from commit #1 — strict typing, lint/format, and dependency pinning all pass green before business logic exists (per PROJECT.md core value).
Output: `pyproject.toml`, `uv.lock`, and four packages under `src/` (banking_client, mcp_server, agent, common) with `__init__.py`, `py.typed` markers, and three `common/` stub modules.
</objective>

<phase_goal>
**As a** developer cloning this capstone repo, **I want to** run `uv sync` and have `mypy --strict src/` and `ruff check src/` report zero errors on a four-package skeleton, **so that** I can demonstrate enterprise-grade production tooling posture before writing any business logic.
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-skeleton-tooling/01-CONTEXT.md
@.planning/phases/01-package-skeleton-tooling/01-RESEARCH.md
@.planning/phases/01-package-skeleton-tooling/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols and files created by this phase (excluded from drift verification — they do not exist yet):

**New files:**
- `pyproject.toml` — single project config (build, deps, tool config)
- `uv.lock` — resolved dependency lock file
- `src/banking_client/__init__.py`, `src/banking_client/py.typed`
- `src/mcp_server/__init__.py`, `src/mcp_server/py.typed`
- `src/agent/__init__.py`, `src/agent/py.typed`
- `src/common/__init__.py`, `src/common/py.typed`
- `src/common/config.py`, `src/common/logging.py`, `src/common/errors.py`

**New symbols:**
- `__version__` (module-level str) in each of the four `__init__.py` files
- `__all__` (list[str]) in `src/common/__init__.py`
- `get_config(key: str, default: str | None = None) -> str | None` in `src/common/config.py`
- `OpenBankingError(Exception)` in `src/common/errors.py`
- `ConfigurationError(OpenBankingError)` in `src/common/errors.py`
- `AuthenticationError(OpenBankingError)` in `src/common/errors.py`
- a logging stub function/symbol in `src/common/logging.py` (e.g. `get_logger`)

**New config keys (pyproject.toml):**
- `[build-system]`, `[project]`, `[project.optional-dependencies] dev`
- `[tool.hatch.build.targets.wheel] packages`
- `[tool.mypy]` (strict, python_version, mypy_path, packages)
- `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.lint.pydocstyle]`
- `[tool.pytest.ini_options]`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pyproject.toml with build backend, pinned deps, and all strict tool config</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-package-skeleton-tooling/01-RESEARCH.md (read the "Complete pyproject.toml Structure" code example and Open Questions 1-3 for project name/version/dep-group recommendations)
    - .planning/phases/01-package-skeleton-tooling/01-CONTEXT.md (D-04 through D-09, D-11 — ruff rules, Google convention, py312 target, line length 120, flat packages, single project, version source of truth)
    - .planning/phases/01-package-skeleton-tooling/01-PATTERNS.md ("pyproject.toml (config)" section listing required sections)
  </read_first>
  <action>
    Create `pyproject.toml` at the repo root as a single-project config. Set `[build-system]` requires = ["hatchling"], build-backend = "hatchling.build". Set `[project]` name = "open-banking-mcp" (per RESEARCH Open Question 1 recommendation — this literal distribution name is what every `importlib.metadata.version()` call in Task 2 must use), version = "0.1.0", description = "Open Banking MCP Capstone", requires-python = ">=3.12". Add `[project.dependencies]` with the single pinned entry mcp>=1.27,<2 (TOOL-05 — pinned but unused). Add a dev dependency group containing mypy>=1.16, ruff>=0.11, pytest>=8, pytest-asyncio>=1, pytest-cov>=7 (use `[project.optional-dependencies] dev = [...]` for broad compatibility per RESEARCH Open Question 2).

    Wire the build (TOOL-01): `[tool.hatch.build.targets.wheel]` with packages explicitly listing all four — src/banking_client, src/mcp_server, src/agent, src/common (per D-09 and the anti-pattern warning: never rely on hatchling auto-discovery for multi-package src layout).

    Configure mypy (TOOL-02): `[tool.mypy]` with strict = true, python_version = "3.12", mypy_path = "$MYPY_CONFIG_FILE_DIR/src", and packages = ["banking_client", "mcp_server", "agent", "common"]. The mypy_path setting is the key link that lets mypy resolve cross-package imports under src/ (Pitfall 2).

    Configure ruff (TOOL-03, D-04 through D-07): `[tool.ruff]` with target-version = "py312", line-length = 120, src = ["src"]. `[tool.ruff.lint]` select = ["E", "F", "I", "B", "UP", "SIM", "D"]. `[tool.ruff.lint.pydocstyle]` convention = "google".

    Configure pytest (TOOL-04): `[tool.pytest.ini_options]` with testpaths = ["tests"] and asyncio_mode = "auto". No tests are added this phase; config only.

    Do NOT add coverage gate / CI / pre-commit config — those are Phase 2.
  </action>
  <verify>
    <automated>test -f pyproject.toml && python3 -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='open-banking-mcp', d['project']['name']; assert d['build-system']['build-backend']=='hatchling.build'; assert any('mcp' in x and '1.27' in x for x in d['project']['dependencies']), d['project']['dependencies']; assert d['tool']['mypy']['strict'] is True; assert set(['E','F','I','B','UP','SIM','D']).issubset(set(d['tool']['ruff']['lint']['select'])), d['tool']['ruff']['lint']['select']; assert d['tool']['ruff']['lint']['pydocstyle']['convention']=='google'; assert d['tool']['ruff']['line-length']==120; assert d['tool']['pytest']['ini_options']['asyncio_mode']=='auto'; assert len(d['tool']['hatch']['build']['targets']['wheel']['packages'])==4; print('pyproject OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml parses as valid TOML (python3 tomllib loads it without error)
    - [project] name == "open-banking-mcp" and version == "0.1.0"
    - [build-system] build-backend == "hatchling.build"
    - [project.dependencies] contains an mcp entry pinned >=1.27,<2 (TOOL-05)
    - dev dependency group contains mypy, ruff, pytest, pytest-asyncio, pytest-cov
    - [tool.hatch.build.targets.wheel] packages lists exactly 4 entries (all under src/) (TOOL-01)
    - [tool.mypy] has strict = true, python_version = "3.12", mypy_path set to the src dir, and a 4-package list (TOOL-02)
    - [tool.ruff].lint select is a superset of [E,F,I,B,UP,SIM,D] and pydocstyle convention == "google" and line-length == 120 (TOOL-03, D-04..D-07)
    - [tool.pytest.ini_options] asyncio_mode == "auto" (TOOL-04)
    - No CI, pre-commit, or coverage-fail-under config present (deferred to Phase 2)
  </acceptance_criteria>
  <done>pyproject.toml exists at repo root with hatchling build backend, mcp pin, dev deps, and strict mypy/ruff/pytest configuration matching all locked decisions D-04 through D-09 and D-11.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create four src-layout packages with typed __init__, py.typed markers, and common stubs</name>
  <files>src/banking_client/__init__.py, src/banking_client/py.typed, src/mcp_server/__init__.py, src/mcp_server/py.typed, src/agent/__init__.py, src/agent/py.typed, src/common/__init__.py, src/common/py.typed, src/common/config.py, src/common/logging.py, src/common/errors.py</files>
  <read_first>
    - .planning/phases/01-package-skeleton-tooling/01-PATTERNS.md (Pattern Assignments — exact code for each __init__.py, config.py, errors.py, and the logging.py stdlib-shadow rule)
    - .planning/phases/01-package-skeleton-tooling/01-RESEARCH.md (Patterns 1-4, the errors.py / minimal __init__.py / config.py code examples, and Pitfalls 1, 3, 4)
    - .planning/phases/01-package-skeleton-tooling/01-CONTEXT.md (D-01, D-02, D-03, D-10, D-11, D-12 — stub depth, type annotations + Google docstrings, re-exports, module docstring + __version__, dynamic version, py.typed markers)
    - pyproject.toml (created in Task 1 — confirm distribution name "open-banking-mcp" for importlib.metadata.version calls)
  </read_first>
  <action>
    Create the four flat packages under src/ (D-08): banking_client, mcp_server, agent, common.

    For banking_client, mcp_server, and agent (PKG-02, PKG-03, PKG-04): each gets an `__init__.py` containing a one-line module docstring describing the package purpose (D-10), an `import importlib.metadata`, and a `__version__: str` read via `importlib.metadata.version("open-banking-mcp")` wrapped in try/except `importlib.metadata.PackageNotFoundError` falling back to "0.0.0" (D-11, Pattern 1). Use the literal distribution name "open-banking-mcp" — NOT the package name and NOT `__name__` (Pitfall 1: package names are not distributions and raise PackageNotFoundError). mcp_server and agent contain ONLY `__init__.py` plus `py.typed` (intentionally empty packages). Add an empty `py.typed` marker file to all four packages (D-12, PKG-01 PEP 561).

    For common (PKG-05), create three stub modules (D-01 — stubs only, no working implementations; D-02 — full type annotations + one-line Google-style docstrings):
    - `errors.py`: module docstring; class OpenBankingError(Exception) as the base, plus ConfigurationError(OpenBankingError) and AuthenticationError(OpenBankingError), each with a one-line class docstring (per RESEARCH errors.py example).
    - `config.py`: module docstring; `from __future__ import annotations`; one stub function get_config(key: str, default: str | None = None) -> str | None with a Google-style docstring (Args/Returns) returning default (per RESEARCH Pattern 2).
    - `logging.py`: module docstring; `from __future__ import annotations`; a stub function (e.g. get_logger) with full annotations and a Google-style docstring. CRITICAL (Pitfall 4 / D-02): inside this file never use bare `import logging` — it shadows the stdlib module within the common package scope; if stdlib logging is referenced, alias it as `import logging as _stdlib_logging`.

    Create `common/__init__.py` (D-03, Pattern 3): module docstring "Shared utilities for Open Banking MCP."; `import importlib.metadata`; `from common.errors import OpenBankingError`; the same dynamic `__version__` block; and `__all__: list[str] = ["OpenBankingError", "__version__"]` so downstream packages can do `from common import OpenBankingError`. Add `common/py.typed`.

    Every module must have a module-level docstring and every public function/class must have a Google-style docstring (Pitfall 3 — ruff D rules require docstrings on all public modules/classes/functions; missing ones produce D1xx violations). Do not create a tests/ directory or any test files (Phase 2).
  </action>
  <verify>
    <automated>for f in src/banking_client/__init__.py src/banking_client/py.typed src/mcp_server/__init__.py src/mcp_server/py.typed src/agent/__init__.py src/agent/py.typed src/common/__init__.py src/common/py.typed src/common/config.py src/common/logging.py src/common/errors.py; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done; grep -q 'open-banking-mcp' src/banking_client/__init__.py && grep -q 'OpenBankingError' src/common/errors.py && grep -q 'from common.errors import OpenBankingError' src/common/__init__.py && grep -q '__all__' src/common/__init__.py && ! grep -nE '^[[:space:]]*import logging([[:space:]]|$)' src/common/logging.py && echo "structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 files exist (4 __init__.py, 4 py.typed, config.py, logging.py, errors.py)
    - py.typed files exist in all four package roots (PEP 561, D-12)
    - mcp_server/ and agent/ contain only __init__.py and py.typed (no other .py files) (PKG-03, PKG-04)
    - Each __init__.py contains a module docstring, `import importlib.metadata`, and `importlib.metadata.version("open-banking-mcp")` with PackageNotFoundError fallback (D-10, D-11)
    - No __init__.py uses __name__ or a package-name literal inside version() (Pitfall 1)
    - src/common/errors.py defines OpenBankingError(Exception), ConfigurationError, AuthenticationError each with a docstring (D-01, D-02)
    - src/common/config.py defines get_config(key: str, default: str | None = None) -> str | None with a Google-style docstring (D-01, D-02)
    - src/common/__init__.py contains `from common.errors import OpenBankingError` and `__all__` including "OpenBankingError" (D-03)
    - src/common/logging.py contains no line matching a bare `import logging` statement (Pitfall 4)
    - No tests/ directory created (Phase 2 scope)
  </acceptance_criteria>
  <done>Four importable src-layout packages exist with typed __init__ files, py.typed markers, and three annotated Google-docstring stub modules in common; the re-export `from common import OpenBankingError` is wired via __all__.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sync the environment and prove mypy --strict and ruff pass green end-to-end</name>
  <files>uv.lock</files>
  <read_first>
    - pyproject.toml (Task 1 — the config being exercised)
    - .planning/phases/01-package-skeleton-tooling/01-RESEARCH.md (Pitfalls 2, 3, 5 — mypy src resolution, ruff D rules, hatchling discovery; Environment Availability table confirming uv 0.9.20 / Python 3.12 present)
    - .planning/phases/01-package-skeleton-tooling/01-PATTERNS.md (shared patterns to confirm before running gates)
  </read_first>
  <action>
    Run `uv sync` from the repo root to resolve dependencies, generate `uv.lock`, and install the open-banking-mcp project plus dev tools into the src-layout virtualenv (PKG-01). This is the end-to-end happy path of the Walking Skeleton — after this, the full local toolchain runs green.

    Then run the three quality gates and drive each to zero errors:
    1. `uv run mypy --strict src/` — must report zero errors (success criterion 3). If "Cannot find implementation or library stub" appears, the mypy_path/packages config in pyproject.toml is the cause (Pitfall 2) — fix the config, not the source.
    2. `uv run ruff check src/` — must report zero violations. D1xx docstring violations mean a module/class/function is missing a Google-style docstring (Pitfall 3) — add the docstring in Task 2's files.
    3. `uv run ruff format --check src/` — must report all files already formatted.

    Also confirm all four packages import cleanly from an installed session (`uv run python -c "import banking_client, mcp_server, agent, common; from common import OpenBankingError"`) — proves hatchling discovered all four (Pitfall 5) and the common re-export works (D-03). Commit `uv.lock` so the resolved environment is reproducible (supply-chain pinning, threat T-01-SC).

    Do not run pytest with a coverage gate (no tests this phase; coverage gate is Phase 2).
  </action>
  <verify>
    <automated>uv sync && uv run mypy --strict src/ && uv run ruff check src/ && uv run ruff format --check src/ && uv run python -c "import banking_client, mcp_server, agent, common; from common import OpenBankingError; print('imports OK', banking_client.__version__)" && test -f uv.lock && echo "GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` exits 0 and creates uv.lock (PKG-01)
    - `uv run mypy --strict src/` exits 0 with zero errors (ROADMAP success criterion 3)
    - `uv run ruff check src/` exits 0 with zero violations (ROADMAP success criterion 4)
    - `uv run ruff format --check src/` exits 0 (all files already formatted)
    - `uv run python -c "import banking_client, mcp_server, agent, common; from common import OpenBankingError"` exits 0 (all four packages discovered by hatchling; common re-export works)
    - uv.lock exists at repo root
  </acceptance_criteria>
  <done>`uv sync` resolves and installs the project; `mypy --strict src/`, `ruff check src/`, and `ruff format --check src/` all report zero errors; all four packages import from a clean session — the Walking Skeleton tooling passes green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → local build env | Third-party packages (hatchling, mypy, ruff, pytest*, mcp) are downloaded and installed during `uv sync` |
| pyproject.toml → installed environment | Declared dependency specifiers are resolved into concrete installed versions |

## STRIDE Threat Register

ASVS Level: 1 — Block on: high. This phase is scaffolding only; no auth, sessions, input handling, or crypto exist (per RESEARCH Security Domain: V2–V6 all N/A).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `uv sync` package installs (hatchling, mypy, ruff, pytest, pytest-asyncio, pytest-cov, mcp) | medium | mitigate | Pin `mcp>=1.27,<2`; commit `uv.lock` (Task 3) so resolved versions are reproducible and auditable. All seven packages reviewed in RESEARCH Package Legitimacy Audit — all canonical PyPA/python/Astral/pytest-dev/Anthropic-maintained packages; SUS verdicts are documented false positives (PyPI download data unavailable to tooling), none [ASSUMED]/[SLOP]. No blocking human-verify checkpoint required. |
| T-01-02 | Tampering | Build backend (hatchling) supply chain | low | accept | hatchling is PyPA-maintained (github.com/pypa/hatch); pinned transitively via uv.lock. Risk accepted — no in-house build code, standard ecosystem backend. |
| T-01-03 | Elevation | Malicious package postinstall scripts during `uv sync` | low | accept | All dependencies are long-established (4–15 yr) canonical packages from known orgs per the legitimacy audit; uv resolves from PyPI with lock pinning. Risk accepted for scaffolding phase. |
</threat_model>

<verification>
Phase-level checks (run from repo root):

1. `python3 -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` — pyproject.toml is valid TOML
2. `uv sync` — exits 0, installs project in src-layout venv, writes uv.lock (PKG-01)
3. `uv run mypy --strict src/` — zero errors (TOOL-02, success criterion 3)
4. `uv run ruff check src/` — zero violations (TOOL-03, success criterion 4)
5. `uv run ruff format --check src/` — all files formatted (TOOL-03)
6. `uv run python -c "import banking_client, mcp_server, agent, common; from common import OpenBankingError"` — all four packages importable, common re-export works (PKG-02..05, D-03)
7. `ls src/*/py.typed | wc -l` returns 4 — PEP 561 markers present (D-12)
</verification>

<success_criteria>
- [ ] `uv sync` completes without errors and installs open-banking-mcp in a src-layout virtualenv (PKG-01)
- [ ] Four packages exist under src/ (banking_client, mcp_server, agent, common) each with __init__.py; mcp_server and agent contain only __init__.py + py.typed (PKG-02, PKG-03, PKG-04)
- [ ] common/ contains config.py, logging.py, errors.py stubs with type annotations + Google docstrings (PKG-05, D-01, D-02)
- [ ] `mypy --strict src/` reports zero errors (TOOL-02)
- [ ] `ruff check src/` and `ruff format --check src/` report zero violations (TOOL-03)
- [ ] pyproject.toml declares hatchling backend (TOOL-01), pytest config (TOOL-04), and mcp>=1.27,<2 pin (TOOL-05)
- [ ] `from common import OpenBankingError` succeeds (D-03)
- [ ] py.typed markers present in all four packages (D-12)
</success_criteria>

<output>
Create `.planning/phases/01-package-skeleton-tooling/01-01-SUMMARY.md` when done.
</output>
